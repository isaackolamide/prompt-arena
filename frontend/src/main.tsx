import React from 'react';
import ReactDOM from 'react-dom/client';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <div>
      <h1>Prompt Arena</h1>
      <p id="welcome-message">Welcome to Prompt Arena</p>
    </div>
  </React.StrictMode>
);
